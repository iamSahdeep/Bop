<!-- This default style sheet sucks.
     Ignore this - check out the file `values-v11/styles.xml`
 -->

<resources>

        <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
            <item name="android:windowBackground">@drawable/splashview</item>
        </style>

    <style name="lightTheme" parent="MaterialDrawerTheme.Light.TranslucentStatus">
        <item name="colorPrimaryDark">@color/primaryDarkColor</item>
        <item name="colorPrimary">@color/primaryColor</item>
    </style>

    <style name="darkTheme" parent="MaterialDrawerTheme.TranslucentStatus">
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryDarkColor</item>
        <item name="colorAccent">@color/primaryLightColor</item>
        <item name="actionModeBackground">@color/primaryColor</item>
        <item name="actionModeCloseDrawable">@drawable/ic_cancel</item>
    </style>
</resources>